def __GENERATED__(self, t, obj, ctx, /):
    try:
        v_0 = obj.x if isinstance(obj.x, int1) else serialize(self, int1, obj.x, ctx)
        v_1 = obj.y if isinstance(obj.y, int1) else serialize(self, int1, obj.y, ctx)
        return {'x': v_0,'y': v_1}
    except ValidationError:
        raise
    except Exception as exc:
        raise ValidationError(exc=exc, ctx=ctx)
